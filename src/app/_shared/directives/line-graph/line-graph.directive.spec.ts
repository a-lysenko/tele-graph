import { LineGraphDirective } from './line-graph.directive';

describe('LineGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new LineGraphDirective({} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
